/**
 * The "api" package (and subpackages) include *only* those pages which are intended to interact
 * with a 3rd party agent, such as the tenant command-and-control api, sqrl devices, open-id, etc.
 *
 * With very few exceptions, all of these pages are expected to render mechanically digestable
 * content (or diagnostic messages), as opposed to user-viewable web pages.
 */
package com.allogy.qrauth.server.pages.api;
